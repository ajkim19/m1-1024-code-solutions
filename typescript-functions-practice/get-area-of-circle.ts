/* exported getAreaOfCircle */
let getAreaOfCircle: (radius: number) => number = (radius) =>
  Math.PI * radius * radius;

// let subtract: (minuend: number, subtrahend: number) => number = (a, b) => a - b;
